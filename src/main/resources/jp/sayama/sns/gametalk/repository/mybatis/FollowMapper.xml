<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.sayama.sns.gametalk.repository.mybatis.FollowMapper">

    <insert id="follow">
        INSERT INTO followers (follower_user_name, followed_user_name)
        VALUES (#{follower}, #{followed})
    </insert>

    <delete id="unfollow">
        DELETE FROM followers
        WHERE follower_user_name = #{follower}
        AND followed_user_name = #{followed}
    </delete>

    <select id="isFollowing" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM followers
        WHERE follower_user_name = #{follower}
        AND followed_user_name = #{followed}
    </select>

    <select id="findFollowers" resultType="string">
        SELECT follower_user_name
        FROM followers
        WHERE followed_user_name = #{userName}
    </select>

    <select id="findFollowing" resultType="string">
        SELECT followed_user_name
        FROM followers
        WHERE follower_user_name = #{userName}
    </select>
    
    <select id="countFollowers" resultType="int">
  	SELECT COUNT(*) FROM followers WHERE followed_user_name = #{userName}
	</select>

	<select id="countFollowing" resultType="int">
  	SELECT COUNT(*) FROM followers WHERE follower_user_name = #{userName}
	</select>
    
</mapper>